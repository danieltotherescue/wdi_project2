<h1>Today's Comments</h1>

<ul>
    <% @comments.each do |comment| %>
        <li>
            <strong> <%= Comment.username %> </strong> - <%= Comment.content %>
	     <%= link_to "More info", comment_path(comment) %>
        </li>
    <% end %>
</ul>
